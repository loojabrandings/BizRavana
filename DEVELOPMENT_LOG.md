# Bizravana — Development Log & Guidelines

---

## Tech Stack

| Layer | Technology |
|-------|-----------|
| **Frontend** | Next.js 16 + React 19 + TypeScript |
| **UI** | shadcn/ui + Tailwind CSS v4 |
| **Animations** | Framer Motion + Tailwind CSS transitions |
| **Backend** | Supabase |
| **Database** | PostgreSQL (Supabase) |
| **Authentication** | Supabase Auth (email/password) |
| **Storage** | Supabase Storage |
| **Hosting** | GitHub + Vercel |
| **Charts** | Recharts |
| **Forms** | React Hook Form + Zod |
| **Tables** | TanStack Table |
| **Icons** | Lucide React |
| **Validation** | Zod |
| **Data Fetching** | TanStack Query (React Query) |
| **Global State** | Zustand (minimal) |
| **Theming** | next-themes + OKLCH CSS variables |
| **PDF** | jsPDF + jsPDF-AutoTable |
| **XLSX** | SheetJS (xlsx) |
| **Image Crop** | react-easy-crop |
| **Fuzzy Search** | Fuse.js |
| **UI Primitives** | Base UI (@base-ui/react) |
| **Realtime** | Supabase Realtime (WebSocket) |

---

## Core Development Guidelines

### 1. General Principles
- Build as a scalable multi-tenant SaaS platform.
- Mobile-First responsive design approach.
- Maintain consistent UI/UX across all pages.
- Use reusable components whenever possible.
- Keep the interface simple, clean, and beginner-friendly.
- Optimize for performance and fast loading.
- Use lazy loading where appropriate.
- Follow clean code principles and modular architecture.

### 2. Database Design
- Design for future scalability from day one.
- Every business-related table must include: `business_id`, `created_by`, `created_at`, `updated_at`
- Business account table must include: `plan_id`, `account_status`, `trial_started_at`, `trial_ends_at`, `subscription_started_at`, `subscription_ends_at`, `data_delete_after`
- Never design tables that only work for a single business.

### 3. Multi-Tenant Architecture
- Every user belongs to a Business.
- Every record must belong to a Business.
- Users should never be able to access another business's data.
- Enforce Row Level Security (RLS) in Postgres.
- All data access through Supabase client.
- Use `business_id` for data isolation across all tables.

### 4. Super Admin Architecture
- Super admin status is stored in JWT `app_metadata.is_super_admin`.
- Helper function `is_super_admin()` reads from JWT, not from profiles table (avoids recursion).
- Admin pages at `/admin/*` route group.
- Admin RLS policies check `is_super_admin()` for SELECT/INSERT/UPDATE/DELETE.
- Admin activity logged to `admin_activity_log` table for audit trail.
- Notifications use service-role admin client for server-side operations requiring RLS bypass.

### 5. Notification Architecture
- Notifications are isolated by `business_id` via RLS.
- Shared `NotificationProvider` at the layout level manages one Realtime channel per business.
- Admin broadcasts insert via server-side API route (bypasses RLS INSERT restrictions).
- Automated notifications generated by pg_cron functions (production) or can be triggered manually.
- Notification rules are seed data in migration 024, manageable from admin UI.
- Realtime subscription uses `postgres_changes` with `business_id` filter for efficiency.

### 6. Component Patterns
- Use shadcn/ui primitives from `src/components/ui/`.
- Reusable business components in `src/components/<domain>/`.
- Shared components (DataTable, PageForm, FilterBar) in `src/components/shared/`.
- Admin components in `src/components/admin/` for mobile-responsive patterns.
- Providers in `src/providers/` for cross-cutting concerns.
- Stores in `src/stores/` for Zustand state management.
- Hooks in `src/hooks/` for reusable React logic.

---

## Development Log

---

## Entry: Smart Customer Parser
**Date:** 2026-07-22

**Summary:**
Built a Smart Customer Parser that extracts structured customer details (name, phone, address, district, city) from unstructured pasted text — primarily WhatsApp messages, SMS, and social media DMs. Designed for Sri Lankan businesses that receive orders via chat and need to avoid manual re-typing.

### Design Discussion (Before Implementation)
The feature was thoroughly discussed with the user before writing code. Key decisions:
- **Scope**: Customer details only (name, phone, address, district, city) — no item parsing
- **Input examples**: 4 real-world WhatsApp messages with varying formats, missing fields, and Sinhala names typed in English
- **Address rule**: Raw pasted text is preserved as-is — nothing is removed from the address even when fields are extracted
- **Auto-fill**: No preview step — parsing happens in the dialog and immediately fills form fields
- **"Rathnapura" edge case**: Common Sinhala transliteration "Rathnapura" (with 'h') needed to match official "Ratnapura" — solved with transliteration normalization

### Parsing Algorithm
1. **WhatsApp Header Stripping** — Regex removes `[time, date] sender:` prefixes from all lines
2. **Line Classification**:
   - Phone line (07XXXXXXXX pattern) → extracted into phone field
   - First non-phone line → customer name
   - District match (against `SRI_LANKA_DISTRICTS` with Sinhala transliteration normalization) → district field (line still included in address)
   - Everything else → address (unchanged, joined as-is)
3. **City Matching (Two-Phase)**:
   - **Forward Match**: If district is identified, the city dropdown is filtered to cities in that district (from courier data). The last address line is checked against the filtered list.
   - **Reverse Lookup**: If no district found, the last word(s) of the last address line are checked against ALL courier cities. If a match is found, the district is inferred from the city's state_id.
   - Supports multi-word city names (tries full line → last 2 words → last word)

### Files Created
- `src/lib/customer-parser.ts` — Pure parsing utility with 6 exported functions
  - `stripWhatsAppHeaders()` — Strips WhatsApp timestamp headers
  - `isPhone()` / `extractPhone()` — Sri Lankan mobile number detection
  - `matchDistrict()` — District matching with transliteration normalization (`th→t`, `dh→d`, `sh→s`)
  - `matchCityInLine()` — Multi-strategy city matching
  - `lookupCityInAllCities()` — Reverse city-to-district lookup from courier data
  - `parseCustomerText()` — Main orchestrator function

### Files Modified
- `src/components/orders/customer-details-section.tsx`:
  - Added "Smart Parser" button (ClipboardPaste icon + label) in the Customer Details section heading
  - Added paste dialog with: dedicated "Paste from clipboard" button (navigator.clipboard.readText()), manual textarea, accuracy warning banner, "Parse & Fill" button
  - Dialog uses consistent button styles matching the form (variant="outline" for Cancel, variant="gradient" for Parse & Fill)
- `src/components/orders/order-form.tsx`:
  - Added "Reset" button in the footer action bar (before Cancel) that clears all customer detail fields + remarks

### Trace Results (4 User Examples)
| Example | Name | Phone | District | City |
|---------|------|-------|----------|------|
| 1: "Kgdlm pathmanayaka ... Rathnapura" | ✅ `Kgdlm pathmanayaka` | ✅ `0711291725` | ✅ Ratnapura (via normalization) | ✅ Balangoda (forward match) |
| 2: "Tharanga sanjeewa ... 0769325472" | ✅ `Tharanga sanjeewa` | ✅ `0769325472` | ✅ Colombo (via Malabe reverse lookup) | ✅ Malabe |
| 3: "MM Nethmi imesha ... deniyaya" | ✅ `MM Nethmi imesha` | ❌ (no phone) | ❌ (fall through) | ❌ (fall through) |
| 4: "W.Hasanka kumara ... 077 9394 568" | ✅ `W.Hasanka kumara` | ✅ `0779394568` | ✅ Colombo (via Nugegoda reverse lookup) | ✅ Nugegoda |

### Implementation Notes
- Phone detection uses regex to find `07XXXXXXXX` within text (not exact line match), handles embedded phones like "Kamal 0771234567"
- District matching has two paths: exact (fast) then normalized (handles transliteration)
- `normalizeForMatch()` strips 'h' after consonants for common Sinhala variations
- City reverse lookup only works when courier city data is available (courier configured)
- The `ClipboardPaste` icon from lucide-react v1.23.0 is used throughout

### Quotation Form Integration
- Smart Parser was also added to the Quotation form (`QuotationCustomerSection`), mirroring the Order form exactly
- Same button, dialog, clipboard paste, accuracy warning, and auto-fill logic
- Warning references "quotation" instead of "order"
- Reset button added to `QuotationForm` footer for clearing customer details
- The shared `parseCustomerText()` utility required no changes — `QuotationFormData` uses the same field names

---

## Entry: Global Notification Management System
**Date:** 2026-07-22

**Summary:**
Complete Notification Management System with admin broadcast creation, automated system notifications, Realtime-powered user bell, and shared NotificationProvider.

### Notification Provider
- **Shared context**: Single `NotificationProvider` at dashboard layout level
- **Realtime subscription**: One Supabase Realtime channel per business, filtered by `business_id`
- **Lifecycle**: Fetch on mount → subscribe → refetch on focus → cleanup channel on unmount
- **Events handled**: INSERT (prepend + increment unread), UPDATE (sync is_read/title/message)
- **Optimistic updates**: markAsRead and markAllAsRead update state immediately, then sync to DB
- **No double-decrement**: Realtime UPDATE handler does NOT decrement unreadCount (optimistic update handles it)

### Admin Notifications Page
- **Overview**: Stats cards (total sent, scheduled, drafts, active rules)
- **Create**: Full dialog with title, message, category, priority, audience (all/active/trial/expired/suspended/by plan), action label/URL, send timing (now/schedule/draft), recipient count preview
- **List**: Responsive table showing sent/scheduled/draft broadcasts with status badges
- **Automated Rules**: Switch-toggle enable/disable, essential rule protection, category+priority badges
- **Delivery**: Server-side API route that inserts notifications for each targeted business owner

### Database Migrations
- **024**: notification_broadcasts, notification_recipients, notification_rules tables + RLS + indexes + new columns on existing notifications (source, priority, category, expires_at, action_label/url, broadcast_id) + 14 seed automated rules
- **025**: Automated notification functions (process_auto_notifications, deliver_scheduled_broadcasts, create_business_notification, notification_already_sent) + hourly/15-min cron schedules

### User Email Fix
- Removed invalid `email` column selection from `profiles` table across 5 admin pages (400 error)
- Added migration 023 with `get_user_emails_by_ids` RPC (SECURITY DEFINER, super admin only)
- Created `/api/admin/user-emails` API route using service role key
- Created `fetchUserEmails()` shared utility for all admin pages
- Admin pages now show real emails from `auth.users`

### Mobile Responsive Admin System
- All 11 admin pages now use shared responsive components:
  - `AdminPageHeader` — responsive header with stackable actions
  - `AdminResponsiveTable` — desktop table >=1024px, mobile cards below
  - `AdminMobileTabs` — horizontally scrollable tabs
  - `AdminSearchBar` — full-width on mobile, compact on desktop
  - `AdminActionSheet` — mobile bottom sheet for actions
  - `AdminMobileRecordCard` — base card structure
- 44px minimum touch targets, no full-page horizontal scroll
- Bottom sheets replace desktop popovers on mobile

---

---

## Entry: Settings Sync to Supabase — Cross-Device Persistence
**Date:** 2026-07-23

**Summary:**
All four operational setting stores (Orders, Quotations, Expenses, Preferences) now persist to the `business_settings` table in Supabase, enabling cross-device settings synchronization. Previously, all settings were stored exclusively in localStorage via Zustand persist — settings made on one device would not appear on another.

### Architecture
- **Storage**: Uses the existing `business_settings` table with `(business_id, key)` unique constraint. Each store's state is stored as a single JSONB value under a well-known key:
  - `orders_settings` — Order prefix, numbering, statuses, payment methods, courier charge, etc.
  - `quotation_settings` — Quotation prefix, numbering, expiry days
  - `expense_settings` — Payment methods list, default method
  - `preferences` — Theme, accent, font, currency, date format, background style
- **Hydration on mount** (`hydrateStoresFromServer`): Called after business ID is known in the dashboard layout. Fetches all 4 rows from Supabase and merges into the Zustand stores. Only non-function fields that exist in the server payload are overwritten (server wins).
- **Auto-save on change** (`setupAutoSync`): Subscribes to Zustand store changes via `.subscribe()`. A dynamic `stripFunctions()` helper uses `Object.fromEntries(Object.entries(state).filter(...))` to strip action methods — auto-adapts to store changes without manual updates. Changes are debounced 1 second before upserting to Supabase.
- **Initial push** (`pushCurrentState`): Runs once on mount to seed the server for first-time users, making settings immediately available on other devices.
- **Cleanup**: On unmount, all subscriptions are unsubscribed and all debounce timers are cleared.

### Files Created
- `src/lib/settings-sync.ts` — Shared utility with `hydrateStoresFromServer()`, `setupAutoSync()`, and internal helpers

### Files Modified
- `src/components/layout/dashboard-layout.tsx` — Extended `fetchUserInfo` useEffect to call `hydrateStoresFromServer()` then `setupAutoSync()` after getting businessId. Cleanup: cancelled flag + unsubscribe + timer clearing.

### Design Decisions
- **Hydration before subscription**: `hydrateStoresFromServer()` runs before `setupAutoSync()` registers subscriptions. This prevents server-loaded data from triggering unnecessary save round-trips.
- **Dynamic function stripping**: Instead of hard-coded destructuring (`const { setFn1, setFn2, ...data } = state`), uses `Object.fromEntries` + `filter` to dynamically exclude functions. Any new action method added to a store is automatically handled — no manual sync code updates needed.
- **Module-level debounce timers**: Shared across all instances but properly cleaned up on unmount. Prevents race conditions between rapid setting changes.
- **localStorage fallback**: Zustand persist still writes to localStorage as a fast local cache. On next mount, Supabase data overrides localStorage (if available).

### Pre-existing Bugs Fixed During Implementation
- `order-form-wizard.tsx` — `paymentMethods` prop was added to `OrderFormWizardProps` but not threaded through to the `StepContent` component, causing the mobile wizard's Payment Section to always use the fallback method list.
- `order-form.tsx:102` — TypeScript error: `defaultPaymentMethod` from the store is `string`, but `payment_method` in `OrderFormData` expects a union type. Added explicit type assertion.

---

---

## Entry: Shipping Label Feature — Complete Removal
**Date:** 2026-07-23

**Summary:**
The entire A5 shipping label PDF system was removed at user request. The feature spanned 6 files across the codebase with 3 integration points.

### Files Deleted
- `src/lib/shipping-label/types.ts` — Data structures
- `src/lib/shipping-label/fetch-data.ts` — Supabase data fetching
- `src/lib/shipping-label/validate.ts` — Input validation
- `src/lib/shipping-label/generate-pdf.ts` — jsPDF A5 generation (barcode, COD, handling instructions)
- `src/components/orders/shipping-label-dialog.tsx` — Dialog UI with preview, print, download

### Files Modified
- `orders/page.tsx` — Removed 6 imports, 3 state variables, `handleRowPrintLabel`, `handleBulkPrintLabels`, Print Label button (mobile card + bulk toolbar), `<ShippingLabelDialog>` JSX
- `courier-settings.tsx` — Removed `HandlingInstruction` imports, ALL_HANDLING_OPTIONS, toggleHandling, shipping label state/loading/save, entire Shipping Label Defaults card
- `order-preview.tsx` — Removed 3 imports, `handlePrintLabel` callback, `onPrintLabel` prop, Shipping Label button (desktop + mobile dropdown)
- `package.json` — Removed `jsbarcode` and `svg2pdf.js` dependencies

### Dependencies Preserved
- `jspdf` and `jspdf-autotable` kept — still used for order/quotation invoice PDFs

---

## Entry: Orphaned Supabase Tables Discovered
**Date:** 2026-07-23

**Summary:**
Cross-referenced the live Supabase schema (CSV export) against all 25 migration files to find tables with no migration coverage.

### Orphaned Tables (3)
| Table | Provider Default | Purpose (inferred) |
|-------|-----------------|--------------------|
| `courier_cities` | `koombiyo_delivery` | Courier city cache with external IDs |
| `courier_districts` | `koombiyo_delivery` | Courier district cache with external IDs |
| `courier_waybills` | `koombiyo_delivery` | Courier waybill pool (available/assigned/used) |

All three default to `koombiyo_delivery` provider, suggesting they were created for a Koombiyo integration that was never completed. None are referenced in `src/` code. Safe to drop.

---

## Entry: WhatsApp Templates — Full-Width Desktop Layout
**Date:** 2026-07-24

**Summary:**
The WhatsApp templates settings page desktop layout was restructured to make both the Saved Templates section and Placeholders section full-width rows instead of a side-by-side layout.

### Before
- Top row was a two-column grid: Template List (260px sidebar) + Placeholders (remaining width)
- Template list was constrained to a narrow left sidebar
- Placeholders occupied the remaining horizontal space beside the template list

### After
- Saved Templates section spans full width as its own row
- Placeholders section spans full width below it
- Editor + Preview remain as two columns in the bottom row
- The template list items now have space to show longer titles without truncation

### Files Modified
- `src/components/whatsapp/whatsapp-templates-settings.tsx` — Restructured the desktop layout grid from `gridTemplateColumns: "260px 1fr"` to two stacked full-width sections

---

*Previous entries truncated for brevity — see full file history for complete log.*
